export type GetEmployees = {
    id: string,
    name: string,
    job: string,
    admission_date: string,
    phone: string,
    image: string
}

export type TibleLineProps = {
    data:GetEmployees
}